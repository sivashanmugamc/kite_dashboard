# Core dependencies
kiteconnect>=4.0.0
pandas>=1.3.0
requests>=2.25.0
nsetools>=1.0.11

# Configuration
configparser

# Standard library modules used extensively
# (These are included with Python, no need to install separately)
# os, json, time, datetime, glob
